# {{ ansible_managed }}
# Do not edit manually unless you know what your doing ;) 

{% for host in hosts.infra %}
[{{ host.cname }}]
{{ host.cname }} ansible_host="{{ host.ip }}" ansible_port=22 ansible_user="{{ host.user }}" ansible_ssh_private_key_file="{{ shelleg.vagrant_dir }}/.vagrant/machines/{{ host.cname }}/virtualbox/private_key" ip="{{ host.ip }}"

{% endfor %}

[{{ hosts.infra_group_name }}]
{% for host in hosts.infra %}
{{ host.cname }}
{% endfor %}

{% for host in hosts.ci %}
[{{ host.cname }}]
{{ host.cname }} ansible_host="{{ host.ip }}" ansible_port=22 ansible_user="{{ host.user }}" ansible_ssh_private_key_file="{{ shelleg.vagrant_dir }}/.vagrant/machines/{{ host.cname }}/virtualbox/private_key" ip="{{ host.ip }}"
{% endfor %}

[{{ hosts.ci_group_name }}]
{% for host in hosts.ci %}
{{ host.cname }}
{% endfor %}

{% for host in hosts.swarm.swarm_managers %}
[{{ host.cname }}]
{{ host.cname }} ansible_host="{{ host.ip }}" ansible_port=22 ansible_user="{{ host.user }}" ansible_ssh_private_key_file="{{ shelleg.vagrant_dir }}/.vagrant/machines/{{ host.cname }}/virtualbox/private_key" ip="{{ host.ip }}"
{% endfor %}

{% for host in hosts.swarm.swarm_workers %}
[{{ host.cname }}]
{{ host.cname }} ansible_host="{{ host.ip }}" ansible_port=22 ansible_user="{{ host.user }}" ansible_ssh_private_key_file="{{ shelleg.vagrant_dir }}/.vagrant/machines/{{ host.cname }}/virtualbox/private_key" ip="{{ host.ip }}"

{% endfor %}

[{{ hosts.swarm.swarm_managers_group_name }}]
{% for host in hosts.swarm.swarm_managers %}
{{ host.cname }}
{% endfor %}

[{{ hosts.swarm_group_name }}]
{% for host in hosts.swarm.swarm_managers %}
{{ host.cname }}
{% endfor %}
{% for host in hosts.swarm.swarm_workers %}
{{ host.cname }}
{% endfor %}

[{{ hosts.swarm.swarm_workers_group_name }}]
{% for host in hosts.swarm.swarm_workers %}
{{ host.cname }}
{% endfor %}